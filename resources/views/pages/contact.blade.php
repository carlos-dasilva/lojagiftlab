@extends('layouts.app') @section('title','Contato — Gift Lab') @section('content')<section class="page-hero compact"><div class="container narrow"><span class="kicker">Vamos conversar</span><h1>Conte sua ideia para a gente.</h1><p>Dúvidas, personalizações ou apenas um “oi”: o laboratório está aberto.</p></div></section><section class="container contact-grid"><div><h2>Fale com a Gift Lab</h2><p>Responderemos assim que possível pelos dados informados.</p><a href="mailto:{{ $siteSettings['email'] }}">✉ {{ $siteSettings['email'] }}</a></div><form method="post" action="{{ route('contact.send') }}" class="form-card">@csrf<input name="website" tabindex="-1" autocomplete="off" class="honeypot"><label>Nome<input name="name" value="{{ old('name') }}" required></label><label>E-mail<input type="email" name="email" value="{{ old('email') }}" required></label><label>Assunto<input name="subject" value="{{ old('subject') }}" required></label><label>Mensagem<textarea name="message" rows="6" required>{{ old('message') }}</textarea></label>@if($errors->any())<div class="form-errors">{{ $errors->first() }}</div>@endif<button class="btn primary">Enviar mensagem →</button></form></section>@endsection
