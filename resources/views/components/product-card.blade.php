<article class="product-card" data-product-id="{{ $product->id }}"><div class="product-media"><a href="{{ route('products.show',$product) }}">@if($product->primaryImage)<img src="{{ Storage::url($product->primaryImage->path) }}" alt="{{ $product->primaryImage->alt ?: $product->name }}" loading="lazy">@else<div class="product-placeholder"><span>✦</span><small>Em criação no laboratório</small></div>@endif</a><button class="favorite-button" data-favorite="{{ $product->id }}" aria-label="Adicionar {{ $product->name }} aos favoritos">♡</button><div class="badges">@if($product->is_new)<span class="badge new">Novidade</span>@endif</div></div><div class="product-info"><div class="product-category-tags">@forelse($product->categories as $category)<a href="{{ route('categories.show',$category) }}">#{{ $category->name }}</a>@empty<span>Gift Lab</span>@endforelse</div><h3><a href="{{ route('products.show',$product) }}">{{ $product->name }}</a></h3><div class="product-card-description markdown-content markdown-content--card">{!! Str::markdown($product->short_description ?? '', ['html_input' => 'strip', 'allow_unsafe_links' => false, 'renderer' => ['soft_break' => "<br>\n"]]) !!}</div><div class="price-row"><div><small>Preço sem frete</small><strong>{{ $product->starting_price ? 'A partir de R$ '.number_format($product->starting_price,2,',','.') : 'Consulte o preço' }}</strong></div><a class="round-link" href="{{ route('products.show',$product) }}" aria-label="Ver {{ $product->name }}">→</a></div></div></article>
