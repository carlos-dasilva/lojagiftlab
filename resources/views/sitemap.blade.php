{!! '<'.'?xml version="1.0" encoding="UTF-8"?'.'>' !!}<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ route('home') }}</loc></url><url><loc>{{ route('catalog') }}</loc></url>@foreach($products as $product)<url><loc>{{ route('products.show',$product) }}</loc><lastmod>{{ $product->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($categories as $category)<url><loc>{{ route('categories.show',$category) }}</loc></url>@endforeach @foreach(['quem-somos','politica-de-privacidade','politica-de-cookies','termos-de-uso'] as $page)<url><loc>{{ route('pages.show',$page) }}</loc></url>@endforeach<url><loc>{{ route('faq') }}</loc></url><url><loc>{{ route('contact') }}</loc></url></urlset>
