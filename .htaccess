<IfModule mod_rewrite.c>
    RewriteEngine On

    # A hospedagem compartilhada da Hostinger usa public_html como raiz.
    # Encaminha todas as requisições para a pasta pública do Laravel.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

